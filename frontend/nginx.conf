server {
    listen 4200;
    server_name _;
    resolver 127.0.0.11 valid=10s;

    root /usr/share/nginx/html;
    index index.html;

    location /api/ {
        set $upstream_auth http://auth:8083;
        rewrite ^/api/(.*)$ /$1 break;
        proxy_pass $upstream_auth;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    location /ingestion/ {
        set $upstream_ingestion http://ingestion:8081;
        rewrite ^/ingestion/(.*)$ /$1 break;
        proxy_pass $upstream_ingestion;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    location /llm/ {
        set $upstream_llm http://llm:8080;
        rewrite ^/llm/(.*)$ /api/$1 break;
        proxy_pass $upstream_llm;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    location /notifier/ {
        set $upstream_notifier http://notifier:8084;
        rewrite ^/notifier/(.*)$ /$1 break;
        proxy_pass $upstream_notifier;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    location / {
        try_files $uri $uri/ /index.html;
    }
}